version: "3.8"

services:
  db:
    image: postgres:15-alpine
    container_name: qualiopi-db
    restart: always
    environment:
      POSTGRES_USER: qualiopi_admin
      POSTGRES_PASSWORD: SecretPassword123
      POSTGRES_DB: qualiopi_saas
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
